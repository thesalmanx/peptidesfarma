/** Offset added to Medusa display_id to produce customer-facing order numbers. */
export const ORDER_NUMBER_OFFSET = 11000

/** Subtotal (in dollars) at which standard shipping becomes free. */
export const FREE_STANDARD_THRESHOLD = 200

/** Subtotal (in dollars) at which 2-day shipping becomes free. */
export const FREE_2DAY_THRESHOLD = 300
